import {BattleCards} from "../BattleCards";

export const Projects = () => {

    return (
       <section>
           <h2>There are some projects I want to show you</h2>
           <BattleCards/>
       </section>
    )
}
